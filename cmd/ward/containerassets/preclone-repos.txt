# Ward's bundled substrate manifest is intentionally empty. Deployments own
# their repo roster instead of compiling one user's repositories into Ward.
#
# Deployment-provided rows use this whitespace-delimited format:
#   owner/name  image|cache
