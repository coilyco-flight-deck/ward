// Ollama launcher for ward-kdl, mounted at `ward-kdl agents ollama`
// (cli-guard execverb, exec dialect). This baked copy is example-safe: the real
// deployment bundle carries the tower host and API wiring.
wrap ward-kdl agents ollama {
    exec ollama {
        env OLLAMA_HOST {
            value ssm "/example/ollama/host"
        }
    }
    can run list {
        describe "list installed models"
    }
    can run ps {
        describe "list running models"
    }
    can run show {
        describe "show a model's info; pass the model name as args"
    }
    can run run {
        describe "run a model: interactive, or one-shot with a prompt arg"
    }
    can run pull {
        describe "pull a model from the registry; pass the model name"
    }
    can run stop {
        describe "stop a running model; pass the model name"
    }
    can run version {
        describe "print the Ollama version"
    }
    // Back-link footer (ward#309): doc-link regenerates a `## See also` section
    // into this guardfile's reference doc, so it survives every rebuild wipe-proof.
    doc-link "../ward-kdl.md" ward-kdl.md "the build-time authoring layer behind this surface"
    doc-link "../ward-kdl-surface.md" ward-kdl-surface.md "the full generated verb surface, area by area"
}
