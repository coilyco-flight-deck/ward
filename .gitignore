# Go
*.test
*.out
/coverage.out
/cover.html
*.exe
*.dll
*.so
*.dylib

# Build artifacts
/bin/
/dist/
/ward

# mkdocs build output
/site/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Local dev overrides
*.local

# Runtime spec cache fetched by `make lock` / skew checks; never committed (ward#62).
# The committed spec lock is a distinct name (*.swagger.lock.json) and IS tracked.
*.swagger.v1.json
cmd/ward-kdl/main.go

# specverb-gen emits the surface reference beside the Guardfile (the only path
# it knows); the reviewed copy lives at docs/ward-kdl.guardfile.md.
cmd/ward-kdl/ward-kdl.guardfile.md
