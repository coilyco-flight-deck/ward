// skillsmp ADMIN tier - PLACEHOLDER (ward#344). Verbless skeleton for the ward#339
// migration; staged-only via the `.guardfile.kdl.placeholder` suffix (see the
// read sibling). `inherit` (cli-guard#160) composes the whole write tier (which
// composes read). skillsmp exposes no destructive verb, so admin will likely equal
// write (the signoz pattern); whatever it adds lands here when ward#339 wires it.
wrap ward-kdl-admin pkg skillsmp {
    inherit "../ward-kdl-write/ward-kdl.skillsmp.write.guardfile.kdl"
    // TODO(ward#339): tier verbs
}
